<Page x:Class="ImageProcessing.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ImageProcessing"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">

    <Grid>
        <Border Width="350" Height="370" Background="White" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Авторизація" FontSize="26" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                <TextBlock Text="Логін"/>
                <TextBox x:Name="LoginBox" Height="35" Margin="0,5,0,4"/>
                <TextBlock x:Name="LoginError" Text="Поле не може бути порожнім" Foreground="Red" FontSize="11" Margin="0,0,0,8" Visibility="Collapsed"/>

                <TextBlock Text="Пароль"/>
                <PasswordBox x:Name="PasswordBox" Height="35" Margin="0,5,0,4"/>
                <TextBlock x:Name="PasswordError" Text="Поле не може бути порожнім" Foreground="Red" FontSize="11" Margin="0,0,0,12" Visibility="Collapsed"/>

                <Button Content="Увійти" Height="40" Click="Login_Click"/>
                <Button Content="Реєстрація" Height="40" Margin="0,-1,0,0" Click="Register_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
